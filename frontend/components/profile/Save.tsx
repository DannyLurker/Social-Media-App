import { User } from "@/types";
import React from "react";

type Props = {
  userProfile: User | undefined;
};

const Save = ({ userProfile }: Props) => {
  return <div>2</div>;
};

export default Save;

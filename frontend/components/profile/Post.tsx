import { User } from "@/types";
import React from "react";

type Props = {
  userProfile: User | undefined;
};

const Post = ({ userProfile }: Props) => {
  return <div>1</div>;
};

export default Post;
